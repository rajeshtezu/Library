# Useful Code

## Create an array of length "n" with some initial value

```js
const n = 10;

const initialArray = Array.from({ length: n }, (value, i) => 0);

// This will create array of size n with initial value of 0 for each index
```
